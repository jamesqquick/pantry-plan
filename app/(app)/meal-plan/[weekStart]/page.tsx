/**
 * Week content is rendered by MealPlanShell in the layout (URL-driven client fetch).
 * This segment exists so /meal-plan/[weekStart] routes work; no server data needed.
 */
export default function MealPlanWeekPage() {
  return null;
}
